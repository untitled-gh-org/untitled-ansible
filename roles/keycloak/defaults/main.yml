---
keycloak_realm: untitled
keycloak_issuer_uri: "https://auth.{{ domain_name }}/realms/{{ keycloak_realm }}"
keycloak_build_context: "{{ app_data_dir }}/keycloak"
keycloak_db_data_location: "{{ app_data_dir }}/keycloak/keycloak_postgres"
keycloak_image: untitled/keycloak:latest

keycloak_clients:
  - client_id: "{{ vault_keycloak_client_01_id }}"
    client_secret: "{{ vault_keycloak_client_01_secret }}"
    base_url: "https://{{ domain_name }}"
    redirect_uris:
      - "https://bff.{{ domain_name }}/login/oauth2/code/untitled-bff"
    post_logout_redirect_uris: "https://{{ domain_name }}/*"
    backchannel_logout_session_required: true
    backchannel_logout_url: "https://bff.{{ domain_name }}/logout/connect/back-channel/untitled-bff"
  - client_id: "{{ vault_keycloak_client_02_id }}"
    client_secret: "{{ vault_keycloak_client_02_secret }}"
    base_url: "https://immich.{{ domain_name }}"
    redirect_uris:
      - "app.immich:///oauth-callback"
      - "https://immich.{{ domain_name }}/auth/login"
      - "https://immich.{{ domain_name }}/user-settings"
    post_logout_redirect_uris: "+"
    backchannel_logout_session_required: false
    backchannel_logout_url: ""
